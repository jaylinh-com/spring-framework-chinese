{
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "fix": "markdownlint '**/*.md' --ignore node_modules docs/.vuepress -f",
    "lint": "markdownlint '**/*.md' --ignore node_modules docs/.vuepress",
    "docs:build": "vuepress build docs"
  },
  "devDependencies": {
    "vuepress": "^1.5.0",
    "markdownlint-cli": "^0.23.1"
  }
}
